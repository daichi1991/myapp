新規カテゴリ作成
<%= form_with(model: SoundCategory, local: true) do |f|%>

  <%= f.label :category_name %>
  <%= f.text_field :category_name %>

  <%= f.label :image %>
  <%= f.file_field :image %>

  <%= f.submit '登録'%>

<% end %>
