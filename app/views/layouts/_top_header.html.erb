<nav class="navbar navbar-expand navbar-light bg-light">
  <div class="container">
    <%= link_to 'Onjin', '/'%>

    <ul class="navbar-nav">

      <li class="nav-item nav-link">
        <%= link_to 'Onjinとは?', '/about' %>
      </li>

      <% if user_signed_in? %>


      <li class="nav-item dropdown">
        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
          <% unless current_user.image.blank? %>
            <%= image_tag current_user.image.to_s, class:"user-image-mini" %>
          <% else %>
            <%= image_tag 'default_user.jpeg', class:"user-image-mini" %>
          <% end %>
        </a>
        <div class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
          <div class="menu-list">
            <%= current_user.username %>
          </div>
          <div class="menu-list">
            <%= current_user.email %>
          </div>
          <div class="dropdown-divider"></div>
          <% if current_user.artist.present? %>
            <div class="menu-list">
              <%= link_to artist_path(current_user.artist.id),class:"btn btn-light btn-block menu-btn" do %>
                <i class="fas fa-music"></i>
                チャンネル
              <% end%>
              <div class="dropdown-divider"></div>
            </div>
          <% end %>
          <div class="menu-list">
            <%= link_to user_path(current_user.id),class:"btn btn-light btn-block menu-btn" do %>
              <i class="fas fa-user-alt"></i>
              マイページ
            <% end %>
          </div>
          <div class="menu-list">
            <%= link_to edit_user_path(current_user.id),class:"btn btn-light btn-block menu-btn" do %>
              <i class="fas fa-cog"></i>
              設定
            <% end %>
          </div>

        </div>
      </li>

      <li class="nav-item">
        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete ,class:"btn btn-outline-secondary top-header-btn"%>
      </li>


      <% else %>
        <li class="nav-item">
          <%= link_to '新規登録', new_user_registration_path, class:"btn btn-outline-secondary top-header-btn" %>
        </li>

      <li class="nav-item">
        <%= link_to 'ログイン', new_user_session_path, class:"btn btn-outline-secondary top-header-btn"%>
      </li>

      <% end %>
    </ul>
  </div>

</nav>
